/**
 * A basic model for a team. We can change this as needed. For now
 * it is just set up like this for test purposes (getting and displaying
 * teams).
 */
export class Team {
    id: number;
    ownerId: number;
    teamName: string;
    teamDescription: string;
    
    constructor() {}
}
